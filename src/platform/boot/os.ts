/**
 * HOLD/OS identity (UI-2). The name is the design brief's working title
 * ("proposed, not locked" — state.md open decision); both constants live here
 * so a rename at production polish is one edit with no platform surgery.
 * The version is the CONSOLE's version (independent of the archive schema
 * version, which the ARCHIVE INTEGRITY line reports from the data).
 */
export const OS_NAME = 'HOLD/OS'
export const OS_VERSION = '0.1.0'
