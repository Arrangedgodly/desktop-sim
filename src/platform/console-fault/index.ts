/**
 * OS-level fault boundary (HU-1) — the never-a-white-screen floor. main.tsx
 * mounts it around each session tree; see ConsoleFaultBoundary.tsx.
 */
export { ConsoleFaultBoundary } from './ConsoleFaultBoundary'
